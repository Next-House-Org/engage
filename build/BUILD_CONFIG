# BUILD_CONFIG V.4.0
# Description: Centralized Docker image mapping for Engage services

DOCKER_REGISTRY="docker.nexthouse.org/engage"

# Service → Docker image mapping (relative to DOCKER_REGISTRY)
declare -A SERVICES=(
  [lite]="lite"
  [worker]="worker"
  [admin-cli]="admin-cli"
)

